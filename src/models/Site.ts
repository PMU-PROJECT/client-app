class Site {
  city: string;
  image: string;
  name: string;
  region: string;

  constructor(city: string, image: string, name: string, region: string) {
    this.city = city;
    this.image = image;
    this.name = name;
    this.region = region;
  }
}

export default Site;
